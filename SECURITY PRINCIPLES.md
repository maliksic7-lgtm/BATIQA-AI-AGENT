🔒 Security Principles
API keys must never be exposed to frontend.
Secrets must be stored in environment variables.
Guest data should be minimized.
Staff endpoints require authentication.
AI output must be validated before database actions.
AI must not directly execute arbitrary database queries.